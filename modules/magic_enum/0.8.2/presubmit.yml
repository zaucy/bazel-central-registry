matrix:
  platform:
  - centos7
  - debian10
  - ubuntu2004
  - macos
  - windows
tasks:
  verify_targets:
    name: Verify build targets
    platform: ${{ platform }}
    build_targets:
    - '@magic_enum//:magic_enum'
bcr_test_module:
  module_path: test
  matrix:
    platform:
    - centos7
    - debian10
    - ubuntu2004
    - macos
    - windows
  tasks:
    run_test_module:
      name: Run test module
      platform: ${{ platform }}
      build_targets:
      - '@magic_enum//...'
      test_targets:
      - '@magic_enum//...'
